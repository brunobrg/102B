#: kivy 1.9.1
#: import Transition kivy.uix.screenmanager.NoTransition

<PlayScreen>:
  on_enter: root.startGame()

<Bloco>:
  Label:
    text: str(root.value)
    font_size: min(self.height, self.width) / 2.5
